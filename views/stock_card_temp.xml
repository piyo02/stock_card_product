<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="stock_card_temp" model="ir.ui.view">
        <field name="name">stock.card.product_template</field>
        <field name="model">stock.card</field>
        <field name="inherit_id" ref="stock.product_template_form_view_procurement_button"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@name='button_box']" position="inside">
                <button type="object"
                    name="action_open_stock_card"
                    attrs="{'invisible':[('type', '!=', 'product')]}"
                    class="oe_stat_button" icon="fa-building-o">
                    <field name="btn_stock_card" widget="statinfo" string="Stock Card"/>
                </button>
            </xpath>
        </field>
    </record>

</odoo>